/**
 * This file contains the scripts needed to make brisage.html work.
 * Scripts needed (in order) :
 * - sidebar.js
 */

$(document).ready(function() {
    // Sidebar toggler
    addSideBarTrigger($(".hamburger"))
});
